[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./main"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "always"

[env]
PORT = "8080"
GIN_MODE = "release"
